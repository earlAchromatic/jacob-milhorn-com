---
title: JacobMilhorn.com
layout: layouts/base.njk
---





{% component 'components/welcomeMat.vue'%}



{% set postDat -%}
[
{% for post in collections.posts %}
{"title": "{{post.title | safe}}", "url": "{{post.url}}", "excerpt": "{{post.excerpt | safe }}", "feature_image": "{{post.feature_image}}", "tags":[ {% for tag in post.tags %}"{{tag}}" {% if not loop.last %},{% endif %} {% endfor %}]}
{% if not loop.last %}
,
{% endif %}
{% endfor %}
]
{%- endset %}



{% slottedComponent 'components/Section.vue' %}
    {% component 'components/postsGrid.vue', posts=postDat, hydrate="lazy" %}
{% endslottedComponent %}